# v1.0 Gate — external blockers

These items are blocked on external state (live deployments, source-repo fixes, product
decisions), NOT on router code. The router implementation is complete and green.

- [x] hgnc-link live deployment fixed → deployed 2026-06-16 at
      hgnc-link.genefoundry.org (hgnc-link-npm, /health 200, MCP session ✅).
      Flipped `enabled: true` for hgnc in servers.yaml + uncommented GF_HGNC_URL.
      `doctor` confirms reachable (9 tools).
- [x] pubtator-link drops self-prefix (Standard v1) → deleted the
      `transform: { strip_prefix: "pubtator_" }` block from servers.yaml.
      (pubtator-link#57 / PR #64; router PR removing the transform.)
- [x] All 13 backends adopt Tool-Naming Standard v1 → servers.yaml is
      transform-free; `genefoundry-router list-tools` against the live fleet
      shows 183 tools with zero OVER-64 names (verified 2026-06-16).
- [ ] Spec §19 Q2 (Standard v1.1 verbs) decided fleet-wide.
- [ ] First public deploy auth mode chosen (none vs jwt) per spec §19 Q3.
- [ ] `always_visible` pinned set confirmed against the live fleet (spec §19 Q4).
- [ ] Landing-page "Add to Claude" button wired to the deployed /mcp endpoint.
