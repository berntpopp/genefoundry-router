# Batch-2 Adversarial Review — Findings Roll-up (2026-07-03)

Controller-consolidated findings from the 5 parallel adversarial reviewers.
Decisions already locked by controller:
- (a) vep check_upstream_health → RETAG (add `ops`; restore carve-out to {ops,meta}).
- (b) metadome request_/summarize_ → KEEP name-exempt + track rename (gated breaking change).

## G1 (clingen#19 gnomad#24 hgnc#8 mgi#8 litvar#36) — ALL CLEAN
- 0 Critical/Important. L gates correct (exit-code 1, severity, ignore-unfixed, if:always ×2, digest pin, no SARIF residue). M carve-outs exactly {ops,meta}, object-iteration, Tier1(8)+Tier2(10). All test files executed & pass.
- MINOR (pre-existing, out-of-scope): litvar `test_mcp_capabilities.py` spot-checks one tool name rather than a set-equality drift guard. Future hardening; not a Batch-2 regression.

## G4 (mavedb#11 uniprot#13 gencc#15 metadome#6) — ALL PASS
- 0 Critical/Important. All test-file-only (no L, no source). Constants match ground truth, object-tag iteration correct, all executed & pass.
- metadome (decision b) CONFIRMED correct: exempt list documented w/ rename candidates + stale-guard test + genuinely collected (pytestmark mcp, asyncio auto). Domain-exempt per policy #3 (NOT ops/meta). Only these 2 of 11 tools non-canonical; none else wrongly exempted.
- Non-issue note: G4 read MAIN's stale cli.py (v1.1 validator lives on unmerged router #12). Expected pre-merge; not a defect.
- Controller action: strengthen metadome comment with explicit keep-exempt decision + tracking pointer (tiny 1-commit doc change).

## G2 (mondo#6 orphanet#6 hpo#10 panelapp#10) — 3 PASS + 1 fix
- mondo/orphanet/hpo CLEAN (orphanet tightened to Tier-1-only, negative asserts fire; hpo backfill collected+green).
- panelapp#10 IMPORTANT (VERIFIED in PR diff): stray `.claude/.../memory/v0.3.0-beyond-9-complete.md`. FIX (controller git): git rm + add `.claude/` to .gitignore + push.

## G3 (gtex#55 stringdb#15 pubtator#92 genereviews#84)
- genereviews#84 PASS — reference implementation (tag-iteration, {ops,meta}, L Category-D fixed correctly).
- stringdb#15 PASS — L correct, pure test/CI, all 10 tools Tier-1. Minor: docstring claims a carve-out not in code (harmless false-red potential; optional tidy).
- gtex#55 CRITICAL+IMPORTANT (VERIFIED):
  * (Critical false-green) `_OPS_META_CARVE_OUT` is NAME-based (`name not in`), never reads .tags; `fetch` tagged {search} at search_fetch.py:95-99 → router validator hard-fails gtex_fetch. FIX: tag `fetch` with `meta` at source + rewrite M test to tag-iteration.
  * (Important) PR bundles GATED A#54 GENCODE work: branch cut from local main=8c48b7c (not on origin/main=a4c860b). Commit 37ab75b itself is clean. FIX (controller): rebase --onto origin/main 8c48b7c → isolates pure L+M; force-push. GENCODE stays in gated A#54.
- pubtator#92 IMPORTANT (VERIFIED): `_META_TOOL_NAMES`={diagnostics,review_quickstart,workflow_help} name-allowlist; MCP tools carry NO mcp tags (9 tags= hits are FastAPI APIRouter, unrelated) → router validator hard-fails 3. FIX: tag the 3 MCP tools `meta` at source + switch test meta-carveout to tag-based. Keep 11 _ACTION_VERB_EXEMPT domain tools (policy 3, correct).

## G5 (vep#7 spliceailookup#12 autopvs1#48 clinvar#8)
- spliceailookup#12 PASS (warmup correctly {ops}). clinvar#8 PASS.
- vep#7 CRITICAL+IMPORTANT (decision a): test carve-out widened to {ops,meta,diagnostics,health}. FIX: retag check_upstream_health add `ops` at health.py:34 + narrow test to {ops,meta} + add replacement drift-guard (deleted staleness guard had no replacement).
- autopvs1#48 CRITICAL (zero live blast radius): carve-out={ops,meta,admin}; clear_cache already has meta. FIX: narrow to {ops,meta} (test-only). NOTE: autopvs1 N-followon flat-banner is a SEPARATE branch — do not touch here.

## FIX WAVE — APPLIED + VERIFIED (2026-07-03)
- vep#7      ea80535  retag check_upstream_health(+ops) + carve-out {ops,meta} + drift-guard. ci-local 395 pass. CI green.
- autopvs1#48 96072f0 narrow carve-out {ops,meta}; +joserfc bump 94fd289. ci-local 437 pass.
- pubtator#92 bc04c65 tag 3 meta tools + tag-based carve-out. ci-local 1282 pass. CI green (pass=7).
- gtex#55    442bd6b rebase-drop gated GENCODE(8c48b7c) + fetch retag(meta) + tag-based; +joserfc bump 44a4b77. ci-local 368 pass. Force-pushed.
- panelapp#10 551723e drop stray .claude + gitignore. CI green (pass=3).
- metadome#6 9040ea2 ratified keep-exempt decision note. ci-local 312 pass.

## FLEET FINDING — CVE-2026-49852 (joserfc HS256/384/512 empty-HMAC-key), fixable >=1.6.8
- Transitive via authlib (via fastmcp). Fresh Trivy scan (2026-07-03) fails the L gate.
- FIXED in gtex#55 + autopvs1#48 (bumped to 1.7.2). LATENT on the other 19 batch-2 PRs (stale-green from 2026-06-30 scans; a re-push would go red).
- RECOMMEND: fleet-wide joserfc bump (Dependabot owns this) OR authorized fan-out. NOT done autonomously across 19 repos.

## N FOLLOW-ON (Response-Envelope v1)
- genereviews (unwrap {result:{results}}->results), autopvs1 (map {ok,data,error,meta}->flat banner),
  spliceailookup (banner + execution.taskSupport + ~60s latency in _meta): 3 parallel TDD implementers, own branches off main, own PRs. IN FLIGHT.
- N-migration PRs are off main -> their container-scan may hit joserfc CVE-2026-49852; bump joserfc on each N branch during review (autopvs1 N branch confirmed still 1.6.7).
- CI envelope-conformance backfill to already-conformant backends: pending (unit-level wrapper test asserting banner on success + flat error on failure; couple with joserfc bump so backfill PRs are green).

## N MIGRATION PROGRESS (2026-07-03)
- spliceailookup: DONE PR#13 a6d1db0, MAJOR 2.2.1->3.0.0. execution.taskSupport verified real (task=True -> ToolExecution.taskSupport="optional"); typed latency cost_tier/expected_cold_latency_ms (30/60s) in _meta; in-band isError+flat error; envelope.py split for 600-LOC; joserfc 1.7.2. ci-local 341 pass. IN REVIEW.
- genereviews: DONE PR#85 (764 pass). Tools via FastMCP.from_fastapi(); fixed double-wrap at MCP boundary (OpenAPITool.output_schema override; REST untouched); error path returns flat envelope; MAJOR bump. FastMCP 3.2.4 here (can't isError+structuredContent -> in-band flag, fleet-consistent). IN REVIEW.
- genereviews REVIEW: APPROVED (8/8, ci-local 764 live-confirmed, REST provably untouched via mcp_component_fn, unwrap 1:1 across 13 tools, error path returns flat envelope). Important non-blocking: non-HTTP exception -> status_code=500 -> _classify falls to upstream_unavailable/retryable=True (should be internal_error/False). FIX IN FLIGHT.
- autopvs1: PR#49 2682c12 (447 pass). REVIEW: NEEDS-FIX -> (1) BLOCKING joserfc 1.6.7 on this branch (main never got the bump) -> container-scan red; (2) Important ErrorToolResult sets isError only in to_mcp_result(), not on instance (.is_error) -> caching-middleware risk. Mapping/isError-roundtrip/schema/600-LOC/ci-local all verified clean. FIX IN FLIGHT (joserfc bump + instance is_error + test). {ok,data,error,meta}->flat banner via envelope.py (ok_envelope/error_envelope, collection_field hoisting); MAJOR 1.3.1->2.0.0; isError:true (3.4.2); success_output_schema from *Data models; full suite migrated; TDD. IN REVIEW.
- BACKFILL: clingen#20 DONE (template). EB1 in flight: gnomad/clinvar/vep/pubtator (errors.py Pattern A). EB2-4 (envelope.py repos) + litvar queued. Template: scratchpad/envelope-backfill-template.md.
- FLEET OBS: FastMCP version drift (genereviews 3.2.4 vs spliceai/autopvs1 3.4.2) -> isError support varies. Not blocking; note for future.
- ALL 3 N MIGRATIONS DONE+REVIEWED+GREEN: spliceai#13 (approved), genereviews#85 (approved + internal_error fix 638928b, 765 pass), autopvs1#49 (fixed joserfc+is_error 2ddd478, 448 pass).
- spliceailookup REVIEW: APPROVED (7/7; envelope nests correctly, isError round-trips vs installed fastmcp, taskSupport genuine, conftest unwrap does NOT mask — conformance tests assert raw envelope; ci-local 341 reproduced). Non-blocking: structured() sc.get("success",True) is dead-code (should be sc["success"]); only resolve.py has explicit output_schema; errors.py 590/600 LOC.
- FLEET OBS (runbook): clingen exemplar does NOT set MCP isError:true (flat success:false dict only); only N-migrated backends set isError. Backfill test asserts the flat-dict banner (what fleet ships), not isError. isError standardization = future v2.

## GATED DEEP REVIEWS (best-practices, for user's redeploy)
- A gtex#54: VERDICT MERGE-READY. Bug=GENCODE enum v32/v43 vs live v39 (+dataset-aware resolution); fix byte-identical to stranded 8c48b7c + regression tests; scope clean (no version bump->G); GitHub CI all green (372 pass).
  * MINOR follow-up ticket: v10 resolve_gene_ids always does an extra get_genes round-trip even with a correct v39 id (latency only).
  * RUNBOOK NOTE: gtex local main is 1 commit ahead of origin/main = unpushed stranded 8c48b7c. After merging #54, do `git fetch && git reset --hard origin/main` (NOT pull) to avoid carrying the untested commit.
- B litvar#35: VERDICT NEEDS-FIX. Core fixes work (441 pass, 94.95% cov) BUT: (Important) _resolve_rsid_record (variant_service.py:429-432) misattributes wrong variant when no rsid matches -> wrong get_variant_literature; (Important) per-row PMID skip (:345-347) + autocomplete-failure degrade (:425-428) UNTESTED; (Minor) dead except ValidationError :484-485. Clean: no _build_url dangling, no version scope creep, disclaimer preserved, entrypoint fixes correctly excluded (already merged #31/#32/#34). FIX DONE d04536e: fallback now = exact-rsid-match OR sole candidate with rsid=None; mismatching sole candidate -> None (no misattribution). 4 tests (misattribution/degrade/malformed-PMID/legit-fallback), TDD-confirmed. Dead except removed. Cov 95.49%. ci-local 445 pass. -> B#35 MERGE-READY.

## G VERSION SINGLE-SOURCE (workstream) — branch fix/version-single-source each
- hgnc#9  f6473f5 DONE serverInfo 3.4.2->1.0.1, ci-local 164 pass, joserfc 1.7.2.
- mgi#9   4ea7471 DONE serverInfo 3.4.2->0.3.1 +CHANGELOG cut, ci-local 153 pass, joserfc 1.7.2.
- gtex#56 ecb8df0 DONE off origin/main (verified NO 8c48b7c leak; 5 files only). serverInfo 3.4.2->2.0.1, joserfc 1.6.7->1.7.2, ci-local 371 pass.
- spliceai#14 d4de095 DONE off main (indep of N#13). serverInfo 3.4.2->2.2.1, ci-local 337 pass, joserfc already 1.7.2.
- autopvs1 G queued until autopvs1 N review releases the repo.
- N<->G MERGE COORDINATION (runbook): spliceai/autopvs1 N PRs bump MAJOR version + touch __init__; G PRs convert __init__ to importlib.metadata sourcing. __init__ conflicts on merge -> resolve: take G's metadata-sourcing; pyproject MAJOR from N is source of truth. Merge N first, then G.
- NOTE: spliceai/autopvs1 N migrations bump version (MAJOR) in pyproject/__init__; G converts __init__ to importlib.metadata sourcing -> coordinate at merge (G reads pyproject, so merge N first or trivially rebase G).

## BACKFILL RESULTS + CROSS-CUTTING FINDINGS
- clingen#20 (template) DONE. EB3 DONE: mgi#10, gencc#16, mavedb#12, metadome#7 (all green, test-only). EB1/EB2/EB4 in flight.
- FINDING 1 (disclaimer, decide+fix): _meta.unsafe_for_clinical_use is NOT per-call in mgi/mavedb/metadome (surfaced once via capabilities, token-saving); gencc IS per-call. Ratified banner mandates per-call + it's safety-critical (research-use / GDPR). DECISION: per-call required -> fix non-conformant backends in one wave after full list gathered from EB1/EB2/EB4. Backfill tests assert ground-truth now (green + honest).
- EB1 DONE: gnomad#26 (CONFORMANT), clinvar#9 (CONFORMANT), vep#8 (DRIFT), pubtator#93 (DRIFT). EB2 DONE: hgnc#10, uniprot#14, mondo#7, hpo#11 (error path conformant; disclaimer once-via-capabilities). All green, test-only.
- FINDING 0 (BIG — user decision): vep-link + pubtator-link were MIS-CLASSIFIED as already-conformant. vep: success returns body with NO top-level `success` key; failure NESTED `{"error":{...}}` (not flat); run_mcp_tool ctx is required-positional. pubtator: RAISES fastmcp ToolError(JSON str) instead of returning flat; success passthrough (no success/_meta); error key `recovery` not `recovery_action`; _meta.tool absent. => Both need REAL envelope migrations (like the 3 N migrations), NOT just a test. Beyond approved N scope -> RECOMMEND to user.
- FINDING 1 disclaimer NON-per-call full set (so far): mgi, mavedb, metadome, hgnc, uniprot, mondo, hpo (7). Per-call: gencc, gnomad, clinvar, clingen. Large fleet split (safety vs tokens) -> USER DECISION, not autonomous fix.
- FINDING 2 (payload keys, NOTE only): some tools use domain keys (mgi: annotations/systems) not results/result (OQ1). Larger migration; NOT enforced by locking test; flag to user as future v2 decision.
- FINDING 3 (_meta.tool on success): asymmetric fleet-wide (stamped on error path; success relies on tool body). Minor; consistent with clingen exemplar.
- NOTE: errors.py vs envelope.py split fuzzy — several repos co-locate run_mcp_tool in envelope.py; agents adapted.

## FINAL CI SWEEP (2026-07-03): 38 active session PRs ALL GREEN (0 fail, 0 pending).
- Migrations vep#9/pubtator#94/litvar#38; N spliceai#13/genereviews#85/autopvs1#49; G hgnc#9/mgi#9/gtex#56/spliceai#14/autopvs1#50; backfill+disc (14); K clingen#21/#22/#23; O mondo#8/genereviews#86/gnomad#27; gated A gtex#54 / B litvar#35 — all ok.
- pubtator migration #94 in review (last). Superseded backfill closed: vep#8, litvar#37 (pubtator#93 after review).
- The 15 CLEAN batch-2 PRs (not re-pushed) are STALE-GREEN + joserfc-latent (would fail container-scan if re-scanned). Fixed 6 batch-2 already green.

## GATED-CODE PREP (user does redeploy/merge/admin/remote-delete)
- G: COMPLETE all 5 — hgnc#9, mgi#9, gtex#56, spliceai#14, autopvs1#50 (b04af14, all version constants->1.3.1, joserfc bump). #50 body flags #49 coordination (keep G metadata-sourcing; #49 MAJOR pyproject wins on merge).
- K: DONE — #21 harden base compose+loopback (ci-local 389; needed security_opt de-dup too, and a _ComposeLoader for !reset), #22 data-refresh base:main+verify guard (388), #23 rebase+land guidance-manifest+wire clingen://guidance (392, rebase auto-clean). GATED-within-K -> operator: delete data-refresh/snapshot branch; "Allow Actions to create PRs" toggle.
  * RUNBOOK CONFLICT: #21 and H-workstream #18 both edit base-compose ports: line. #21 is the SUPERSET (loopback+full hardening). Merge #21 + close #18 (or merge #18 first -> #21 trivially rebases).
- K: (was) clingen 3 code PRs in flight (harden base compose; data-refresh base:main+verify; rebase+land guidance-manifest). GATED-within-K: stale data-refresh/snapshot branch delete + "Allow Actions to create PRs" admin toggle -> operator.
- O: DO (code): mondo dead-names (in flight), genereviews -f removal (in flight), gnomad docs site (in flight, Pages toggle=operator), orphanet typo-dir DELETED (Task4 done), router bench VERIFIED ~9.79 (Task11; issue-close deferred). DEFER (remote/admin): Dependabot #82 merge, issue admin #40/#27/#49/#3, gnomad Pages source toggle. Task3 panelapp .claude already handled by panelapp#10.
- F (branch cleanup): GATED + cannot run yet (destructive remote; must run AFTER B/K merge so unique-work branches land; ~50 new PR branches would muddy classification). -> runbook post-merge step. Classifier = git cherry per branch; candidates = old chore/container-hardening-v1 gate-reverting set + stale branches; new PR branches auto-delete on their own merge.

## USER DECISION 2026-07-03: STANDARDIZE BOTH fleet-wide (authorized)
- (1) Envelope migrations for the 3 mis-classified: vep, pubtator, litvar -> flat banner (MAJOR bump, TDD, feat/response-envelope-v1). litvar#38 DONE (6c8faa1: new envelope.py+errors.py run_tool, all 6 tools incl capabilities, isError verified 3.4.2, MAJOR 2.0.1->3.0.0, cov 95.81%, 442 pass) IN REVIEW. vep#9 DONE (4371573: nested error->flat +recovery_action enum, per-call disclaimer force-set, isError 3.4.2, MAJOR 0.3.1->1.0.0, 402 pass) IN REVIEW. pubtator IN FLIGHT. Close superseded backfill PRs (vep#8, pubtator#93, litvar#37) after.
  * litvar#38 REVIEW: APPROVED (all 6 tools incl capabilities wired, isError verified 3.4.2, no #35 conflict, cov 95.81%). Minor: ErrorCode is Literal not Enum; get_variant_summary narrows unused fields. RUNBOOK: merge #35 (resolve_rsid) before #38, then trivial rebase.
  * vep#9 REVIEW: APPROVED (nested->flat loses nothing, per-call disclaimer force-set, isError verified 3.4.2, all paths wired, 402 pass). "Important" nit = DELIBERATE tradeoff: tool fns keep `-> dict` annotation (not `dict|ToolResult`) to preserve output_schema inference; mypy-suppressed. NO fix (union would regress output_schema).
- FUTURE v2 (isError everywhere): migrated backends (spliceai/autopvs1/litvar/vep on fastmcp 3.4.2) set MCP isError:true; older conformant ones (clingen/gnomad/...) return flat success:false WITHOUT isError; genereviews (3.2.4) cannot. Fleet inconsistency -> future standardization, NOT done now.
- (2) Per-call unsafe_for_clinical_use across the 8 once-via-capabilities: mgi, mavedb, metadome, hgnc, uniprot, mondo, hpo, orphanet (additive, folded into their chore/envelope-conformance-test branches; flip test to assert presence; fix exact-key-set tests e.g. orphanet). DISC-A DONE: mgi#10, mavedb#12, metadome#7 (per-call on success+error, minimal-mode preserved via never-stripped invariant, backfill tests flipped, ci-local green). DISCLAIMER WAVE COMPLETE (all 8, additive, ci-local green): mgi#10, mavedb#12, metadome#7 (A); mondo#7, orphanet#7 (C); hgnc#10, uniprot#14, hpo#11 (B). Minimal-mode stripping handled where present (mavedb, metadome, mondo, orphanet, hpo); exact-key-set tests updated everywhere. => per-call unsafe_for_clinical_use now fleet-wide.
  * FOLLOW-UP (minor): orphanet test `test_readme_places_research_warning_in_discovery_not_every_payload` name/intent now contradicts per-call reversal (still passes; README wording + test name to update later).
- Already per-call conformant (no change): gencc, gnomad, clinvar, clingen, panelapp, gtex + the 3 N-migrated (spliceai, genereviews, autopvs1).

## O CODE DONE: mondo#8 (dead-names, TDD), genereviews -f #86, gnomad docs #27 (Pages toggle=operator), orphanet typo-dir DELETED, router bench VERIFIED. O ADMIN deferred: Dependabot#82 merge, issues #40/#27/#49/#3, gnomad Pages toggle.

## DEFERRED — stringdb envelope migration (N)
- Per user instruction: DEFER stringdb's envelope migration until C #14 (stringdb BeforeValidator) merges — it conflicts with stringdb_link/models/responses.py. Tracking note only; NO code now.
- Scope when unblocked (from N brief): add a run_mcp_tool-style wrapper (mirror clingen), rename bare list keys (partners/interactions/...) -> results, convert HTTPException raises -> in-band flat isError envelope, declare output_schema + response_mode=compact, MAJOR bump. Coordinate with the open stringdb homology camelCase / required_score x1000 residual in the same file.

## FIX EXECUTION (historical)
- Parallel fix subagents: vep#7, autopvs1#48, pubtator#92.
- Controller git surgery: gtex#55 (rebase+fetch retag+test), panelapp#10 (rm+gitignore), metadome#6 (comment strengthen).
- No-fix: G1(all), mondo/orphanet/hpo, mavedb/uniprot/gencc, stringdb(minor docstring-optional), genereviews, spliceai, clinvar, metadome-verb-decision(confirmed).
